{% extends 'base.html.twig' %}

{% block content %}
    {% if cmfMainContent.body  %}
        {{ cmfMainContent.body|raw }}
    {% endif %}
    {% if cmfMainContent.additionalInfoBlock  %}
        {{ sonata_block_render({ 'name': 'additionalInfoBlock' }) }}
    {% endif %}
{% endblock %}
